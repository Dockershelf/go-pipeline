name: Update meta-gbp

on:
  workflow_call:
    inputs:
      dists-json:
        required: true
        type: string
      arches-json:
        required: false
        type: string
        default: '["amd64"]'
      publish:
        type: boolean
        default: true
    secrets:
      DEPLOY_SSH_KEY:
        required: false

permissions:
  contents: write
  packages: read

jobs:
  update:
    runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    env:
      DEBFULLNAME: ${{ vars.DEBFULLNAME || 'Luis Alejandro Martínez Faneyth' }}
      DEBEMAIL: ${{ vars.DEBEMAIL || 'luis@luisalejandro.org' }}
      GO_CI_ARCH: ${{ matrix.arch }}
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        arch: ${{ fromJSON(inputs.arches-json) }}
    outputs:
      rev: ${{ steps.rev.outputs.rev }}
      go: ${{ steps.rev.outputs.go }}
      go_repo_dir: ${{ steps.rev.outputs.go_repo_dir }}
    steps:
      - name: Checkout go repo
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Checkout go-pipeline
        uses: actions/checkout@v7
        with:
          repository: Dockershelf/go-pipeline
          path: go-pipeline

      - name: Configure git
        run: |
          git config --global user.name "${DEBFULLNAME}"
          git config --global user.email "${DEBEMAIL}"

      - name: Setup workspace
        run: |
          bash go-pipeline/scripts/ci-setup-workspace.sh \
            "$GITHUB_WORKSPACE" \
            "$GITHUB_WORKSPACE/go-pipeline"

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull builder images
        env:
          PIPELINE_DIR: ${{ github.workspace }}/go-pipeline
          WORKSPACE: ${{ github.workspace }}
        run: bash go-pipeline/scripts/ci-pull-builder-images.sh

      - name: meta-gbp update
        run: go-pipeline/meta-gbp update

      - name: Export revision
        id: rev
        run: |
          echo "rev=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          echo "go=$(echo '${{ github.repository }}' | cut -d/ -f2 | sed 's/^go//')" >> "$GITHUB_OUTPUT"
          echo "go_repo_dir=$GITHUB_WORKSPACE" >> "$GITHUB_OUTPUT"

      - name: Push update commits
        run: |
          git remote set-url origin "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"
          if git rev-parse origin/main >/dev/null 2>&1; then
            ahead="$(git rev-list --count origin/main..HEAD || true)"
          else
            ahead="$(git rev-list --count HEAD || true)"
          fi
          if [[ "${ahead}" != "0" ]]; then
            git push origin HEAD:main
          fi

  build:
    needs: update
    runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    env:
      DEBFULLNAME: ${{ vars.DEBFULLNAME || 'Luis Alejandro Martínez Faneyth' }}
      DEBEMAIL: ${{ vars.DEBEMAIL || 'luis@luisalejandro.org' }}
      GO_CI_ARCH: ${{ matrix.arch }}
      DOCKERSHELF_ARCH: ${{ matrix.arch }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        dist: ${{ fromJSON(inputs.dists-json) }}
        arch: ${{ fromJSON(inputs.arches-json) }}
    steps:
      - name: Checkout go repo
        uses: actions/checkout@v7
        with:
          repository: ${{ github.repository }}
          ref: ${{ needs.update.outputs.rev }}
          fetch-depth: 0

      - name: Checkout go-pipeline
        uses: actions/checkout@v7
        with:
          repository: Dockershelf/go-pipeline
          path: go-pipeline

      - name: Configure git
        run: |
          git config --global user.name "${DEBFULLNAME}"
          git config --global user.email "${DEBEMAIL}"

      - name: Setup workspace
        run: |
          bash go-pipeline/scripts/ci-setup-workspace.sh \
            "$GITHUB_WORKSPACE" \
            "$GITHUB_WORKSPACE/go-pipeline"

      - name: Setup SSH for publish check
        if: vars.DEPLOY_HOST != ''
        uses: webfactory/ssh-agent@v0.10.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}

      - name: Check if already published
        id: published
        if: vars.DEPLOY_HOST != ''
        env:
          DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
          DEPLOY_USER: ${{ vars.DEPLOY_USER }}
          DEPLOY_DIR: ${{ vars.DEPLOY_DIR }}
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          bash go-pipeline/scripts/ci-check-published.sh "${{ matrix.dist }}" "${{ matrix.arch }}"

      - name: Log in to GHCR
        if: vars.DEPLOY_HOST == '' || steps.published.outputs.skip != 'true'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull builder images
        if: vars.DEPLOY_HOST == '' || steps.published.outputs.skip != 'true'
        env:
          PIPELINE_DIR: ${{ github.workspace }}/go-pipeline
          WORKSPACE: ${{ github.workspace }}
        run: bash go-pipeline/scripts/ci-pull-builder-images.sh

      - name: Materialize and build
        if: vars.DEPLOY_HOST == '' || steps.published.outputs.skip != 'true'
        run: |
          go-pipeline/meta-gbp materialize "${{ matrix.dist }}"
          go-pipeline/meta-gbp build

      - name: Upload debs
        if: vars.DEPLOY_HOST == '' || steps.published.outputs.skip != 'true'
        uses: actions/upload-artifact@v7
        with:
          name: debs-${{ matrix.dist }}-${{ matrix.arch }}
          path: go-pipeline/dist/*.deb
          if-no-files-found: error

  smoke:
    needs: [build, update]
    runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    env:
      DOCKERSHELF_ARCH: ${{ matrix.arch }}
    strategy:
      fail-fast: false
      matrix:
        dist: ${{ fromJSON(inputs.dists-json) }}
        arch: ${{ fromJSON(inputs.arches-json) }}
    steps:
      - name: Checkout packaging repo
        uses: actions/checkout@v7
        with:
          repository: ${{ github.repository }}
          ref: ${{ needs.update.outputs.rev }}
          path: packaging-repo
          sparse-checkout: |
            changelogs
            debiandirs

      - name: Checkout go-pipeline
        uses: actions/checkout@v7
        with:
          repository: Dockershelf/go-pipeline
          path: go-pipeline
          sparse-checkout: |
            scripts

      - name: Setup SSH for publish check
        if: vars.DEPLOY_HOST != ''
        uses: webfactory/ssh-agent@v0.10.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}

      - name: Check if already published
        id: published
        if: vars.DEPLOY_HOST != ''
        env:
          DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
          DEPLOY_USER: ${{ vars.DEPLOY_USER }}
          DEPLOY_DIR: ${{ vars.DEPLOY_DIR }}
        working-directory: packaging-repo
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          bash "$GITHUB_WORKSPACE/go-pipeline/scripts/ci-check-published.sh" \
            "${{ matrix.dist }}" "${{ matrix.arch }}"

      - name: Download debs
        if: vars.DEPLOY_HOST == '' || steps.published.outputs.skip != 'true'
        uses: actions/download-artifact@v8
        with:
          name: debs-${{ matrix.dist }}-${{ matrix.arch }}
          path: dist

      - name: Smoke test
        if: vars.DEPLOY_HOST == '' || steps.published.outputs.skip != 'true'
        run: |
          bash go-pipeline/scripts/debian-smoke-test.sh \
            --dist "${{ matrix.dist }}" \
            --go "${{ needs.update.outputs.go }}" \
            --dist-dir dist \
            --arch "${{ matrix.arch }}"

  publish:
    needs: [smoke, update]
    if: inputs.publish && vars.DEPLOY_HOST != ''
    runs-on: ubuntu-latest
    env:
      DOCKERSHELF_ARCH: ${{ matrix.arch }}
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        dist: ${{ fromJSON(inputs.dists-json) }}
        arch: ${{ fromJSON(inputs.arches-json) }}
    steps:
      - name: Checkout packaging repo
        uses: actions/checkout@v7
        with:
          repository: ${{ github.repository }}
          ref: ${{ needs.update.outputs.rev }}
          path: packaging-repo
          sparse-checkout: |
            changelogs
            debiandirs

      - name: Checkout go-pipeline
        uses: actions/checkout@v7
        with:
          repository: Dockershelf/go-pipeline
          path: go-pipeline
          sparse-checkout: |
            scripts
            debian-repo-setup

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.10.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}

      - name: Check if already published
        id: published
        env:
          DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
          DEPLOY_USER: ${{ vars.DEPLOY_USER }}
          DEPLOY_DIR: ${{ vars.DEPLOY_DIR }}
        working-directory: packaging-repo
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          bash "$GITHUB_WORKSPACE/go-pipeline/scripts/ci-check-published.sh" \
            "${{ matrix.dist }}" "${{ matrix.arch }}"

      - name: Download debs
        if: steps.published.outputs.skip != 'true'
        uses: actions/download-artifact@v8
        with:
          name: debs-${{ matrix.dist }}-${{ matrix.arch }}
          path: dist

      - name: Deploy preflight
        if: steps.published.outputs.skip != 'true'
        env:
          DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
          DEPLOY_USER: ${{ vars.DEPLOY_USER }}
          DEPLOY_DIR: ${{ vars.DEPLOY_DIR }}
          DEPLOY_INCOMING: ${{ vars.DEPLOY_INCOMING }}
        run: bash go-pipeline/scripts/ci-deploy-preflight.sh

      - name: Publish to APT droplet
        if: steps.published.outputs.skip != 'true'
        env:
          DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
          DEPLOY_USER: ${{ vars.DEPLOY_USER }}
          DEPLOY_DIR: ${{ vars.DEPLOY_DIR }}
          DEPLOY_INCOMING: ${{ vars.DEPLOY_INCOMING }}
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts
          bash go-pipeline/scripts/ci-publish.sh "${{ matrix.dist }}" dist "${{ matrix.arch }}"
